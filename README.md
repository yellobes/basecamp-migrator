basecamp-migrator Is basically a bunch of python functions that make moving 
projects from the new basecamp api (JSON) to an activecollab api (GET/POST).

The code is pretty self-explanitory.

-Peter
